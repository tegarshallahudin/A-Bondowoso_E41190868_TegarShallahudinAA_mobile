<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tegar.tugas1" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tugas1" >
        <activity android:name=".MenuDashboardActivity" >
        </activity>
        <activity android:name=".Horizontal_ScrollViewActivity" />
        <activity android:name=".Scroll_ViewActivity" />
        <activity android:name=".Material_DesignActivity" />
        <activity android:name=".TabelActivity" />
        <activity android:name=".FrameActivity" />
        <activity android:name=".ConstraintActivity" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>